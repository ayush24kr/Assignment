apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: ai-task-platform
data:
  NODE_ENV: "production"
  MONGO_URI: "mongodb://mongodb:27017/ai-task-platform"
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  CORS_ORIGIN: "https://app.ai-task-platform.com"
  JWT_EXPIRES_IN: "24h"
